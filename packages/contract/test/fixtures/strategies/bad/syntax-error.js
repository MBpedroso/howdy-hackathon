export const meta = { name: 'Broken', rationale: 'I do not parse.', version: 1 };
export function init() { return {}; }
export function decide(view, mem) {
  if (view.tick > 0 { return { type: 'idle' };
}
