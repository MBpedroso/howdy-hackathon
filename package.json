{
  "name": "rematch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.3.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:web": "pnpm --filter @rematch/web dev",
    "dev:server": "pnpm --filter @rematch/server dev",
    "build": "pnpm --filter @rematch/web build",
    "typecheck": "pnpm -r --no-bail run typecheck",
    "test": "pnpm -r run test",
    "test:contract": "pnpm --filter @rematch/contract run test",
    "test:sandbox": "pnpm --filter @rematch/sandbox run test",
    "test:engine": "pnpm --filter @rematch/engine run test",
    "test:harness": "pnpm --filter @rematch/harness run test",
    "test:balance": "pnpm --filter @rematch/harness run test:balance",
    "eval:agents": "pnpm --filter @rematch/agents run eval",
    "harness": "pnpm --filter @rematch/harness cli --",
    "test:e2e": "pnpm --filter @rematch/web run test:e2e",
    "verify": "pnpm run typecheck && pnpm run test"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
